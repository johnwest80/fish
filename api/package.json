{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run lint && npm run build:live",
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "build:live": "nodemon --exec ./node_modules/.bin/ts-node -- ./src/server.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.11.1",
    "body-parser": "^1.18.2",
    "express": "^4.16.3",
    "mongoose": "^5.0.11",
    "ts-node": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^1.17.2",
    "tslint": "^5.9.1",
    "typescript": "^2.7.2"
  }
}
